# STM32_LL_Delay
STM32F103C8T6,基于LL库的毫秒，微秒级延时函数，有阻塞和非阻塞两种版本提供使用。通过systick时基，无需另配定时器。STM32F103C8T6: Millisecond and microsecond delay functions based on the LL library, providing both blocking and non-blocking versions. Utilizing the SysTick time base, no additional timer is required.
